- hosts: webservers
  gather_facts: true
  become: true
  tasks:
  - name: install packages using loops
    apt:
      name: "{{ item }}"
      update_cache: true
      state: latest
    loop:
      - docker.io
      - python-is-python3
      - virtualenv
      - python3-pip 
